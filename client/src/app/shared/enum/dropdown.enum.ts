export const enum Lifecycle {
    ON_DEMAND = 'on-demand',
    SPOT = 'spot'
}

export const enum Region {
    US_EAST_1 = 'us-east-1',
}

export const enum TimeUnit {
    HOURS = "hour",
    DAYS = "day",
    MONTHS = "month"
}